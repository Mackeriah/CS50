// this function takes an argument called color
function changeColor(color)
{
    // it uses whatever was passed for the argument as the background color
    document.getElementById('owen').style.backgroundColor = color;
}